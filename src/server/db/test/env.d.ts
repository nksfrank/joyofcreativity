/// <reference types="@cloudflare/vitest-pool-workers/types" />

// Makes the `cloudflare:test` virtual module (applyD1Migrations, D1Migration, …)
// resolve for the workers-pool db tests under src/server/db/.
